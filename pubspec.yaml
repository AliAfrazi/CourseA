name: course_a_fixed
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  persian_number_utility: ^1.1.2
  provider: ^6.0.5
  google_fonts: ^3.0.1
  card_swiper: ^2.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
    - assets/

  fonts:
    - family: Light
      fonts:
        - asset: assets/fonts/iran sans light.ttf